# Project Rules

- IDs are permanent and must never be reused.
- CLI output must remain Chinese.
- Do not introduce third-party runtime dependencies.
- Every bug fix requires a regression test.
- Prefer the simplest implementation that satisfies the requirements.
- Do not silently change existing CLI behavior.
